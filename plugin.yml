name: pmforms-demo
main: dktapps\pmforms_demo\Main
version: 0.0.1
api: 3.7.2
load: POSTWORLD
author: dktapps
description: Skeleton plugin generated by PocketMine-MP DevTools
website: https://github.com/pmmp/PocketMine-DevTools
commands:
  form:
    description: "Allows sending an example form to the target player"
    usage: "/formdemo <modal|menu|custom> [players ...]"
    permission: pmforms.command
permissions:
  pmforms.command:
    default: true
    description: "Allows the use of /formdemo"
